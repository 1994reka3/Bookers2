<h1>Books#index</h1>

<!-- 新規投稿 -->
<h1>New book</h1>
<%= form_for(@book, url: books_path) do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <p><%= f.submit 'Create Book' %></p>
<% end %>